# Never commit NetworkManager profiles, they contain wifi PSKs
*.nmconnection
system-connections/
*.pem
*.key
id_*
.env
# Pi-hole config holds the dashboard password hash
pihole.toml
# SD card images are huge and contain private keys
*.img
